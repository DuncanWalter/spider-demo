/**
 * `spider-store` hooks and components are
 * testable (refer to the tests for the hooks
 * in `@dwalter/spider-hooks`) but I've not
 * provided any examples here.
 */
test('Jest tests run', () => {
  expect(1).not.toBeFalsy()
})
